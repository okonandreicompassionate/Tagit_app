/**
 * Height of the bottom tab bar in `app/index.tsx`, insets excluded (each
 * screen adds its own `insets.bottom` on top). It floats over whatever's
 * behind it — the camera in particular — so anything with its own
 * bottom-pinned controls needs this to keep clear of it.
 */
export const TAB_BAR_HEIGHT = 74;
